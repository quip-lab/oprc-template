{
    "name": "@repo/db",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "exports": {
        ".": "./src/index.ts",
        "./schema": "./src/schema/index.ts"
    },
    "scripts": {
        "with-env": "dotenv -e ../../.env --",
        "db:generate": "pnpm run with-env drizzle-kit generate",
        "db:migrate": "pnpm run with-env drizzle-kit migrate",
        "db:push": "pnpm run with-env drizzle-kit push",
        "check-types": "tsc --noEmit",
        "lint": "biome lint ."
    },
    "dependencies": {
        "@repo/env": "workspace:*",
        "drizzle-orm": "catalog:",
        "pg": "catalog:"
    },
    "devDependencies": {
        "@repo/typescript-config": "workspace:*",
        "@types/pg": "catalog:",
        "dotenv-cli": "catalog:",
        "drizzle-kit": "catalog:",
        "typescript": "catalog:",
        "vitest": "catalog:"
    }
}
